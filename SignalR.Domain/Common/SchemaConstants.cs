namespace SignalR.Domain.Common;

/// <summary>
/// I prefer to use schema like (dbo) so, I can access items easily
/// </summary>
public struct SchemaConstants
{
    public const string Auth = nameof(Auth);
    public const string Chats = nameof(Chats);
}