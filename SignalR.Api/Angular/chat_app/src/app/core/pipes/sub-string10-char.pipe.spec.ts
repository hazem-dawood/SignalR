import { SubString10CharPipe } from './sub-string10-char.pipe';

describe('SubString10CharPipe', () => {
  it('create an instance', () => {
    const pipe = new SubString10CharPipe();
    expect(pipe).toBeTruthy();
  });
});
